export { default } from "./";

/**
 *
 * 여러 경로에서 같은 화면을 보여주고 싶을 때 사용합니다.
 * 예를 들어,
 * /following (정적 경로)
 * / (즉, index.tsx)
 * 이 두 경로에서 동일한 컴포넌트/화면을 보여주고 싶을 때
 *
 * /following으로 접근해도,
 * 실제로는 index.tsx의 내용을 보여줍니다.
 *
 */
